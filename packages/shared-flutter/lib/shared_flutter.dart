/// shared_flutter
///
/// Placeholder entry point for the shared Flutter package consumed by
/// the Customer App and Employee App (e.g. shared widgets, theming,
/// constants, models).
///
/// Intentionally empty for Phase 1 (repository foundation only).
library shared_flutter;
