# Runtime config for this Flutter app (loaded via flutter_dotenv or
# passed at build time with --dart-define). No secrets belong here —
# Firebase native config lives in google-services.json /
# GoogleService-Info.plist (gitignored), not in this file.

API_BASE_URL=http://localhost:3000/api
FIREBASE_PROJECT_ID=
